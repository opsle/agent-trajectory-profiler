# Benchmarks

Benchmark fixtures and raw results belong here. Each run records exact revisions, configuration, environment, correctness, repetitions, and confounders. No synthetic result should be presented as measured.

Packet-v1 fixtures under `fixtures/context-firewall/` are measurement
conformance cases, not benchmark or AI-performance results.
