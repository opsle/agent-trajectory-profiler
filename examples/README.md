# Examples

Examples must use sanitized synthetic identities and demonstrate generic integration rather than Taslos Tasks internals.

Each checked-in fixture wrapper is directly ingestible and records upstream
revisions, public source identity, packet/source hashes, trajectory events, and
exact expected measurements.
