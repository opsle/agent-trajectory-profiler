# Reference surface

This directory contains a dependency-free, deliberately narrow reference implementation. It demonstrates the interface and supports falsification; it is not a production package.
